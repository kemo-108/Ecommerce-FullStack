import React from "react";
import "./Header.css";

// Reserved for the categories / navigation strip that sits under the main
// header (e.g. "Shop", "Deals", "About", or a dynamic categories list).
// Kept as its own component so it can grow independently from Header.jsx
// instead of duplicating the whole header markup like it used to.
const Navbar = () => {
  return null;
};

export default Navbar;
